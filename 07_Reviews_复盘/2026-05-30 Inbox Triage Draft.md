# 2026-05-30 Inbox Triage Draft

## 今日处理范围

- 复核 `00_Inbox_碎片收集/今日收集.md`。
- 扫描 `00_Inbox_碎片收集` 目录下最近新增或更新的条目。
- 对照项目库、Skill 候选和自动化候选，判断新输入应沉淀到哪里。

## 分类结果

### 1. 碎片 / Inbox

- `今日收集.md` 本身没有新增原始条目。
- 但在 `00_Inbox_碎片收集/价值流作业出图` 发现一个子文件夹和 3 份交付资产，属于前几次未被纳入整理的新输入。

### 2. 项目笔记

- 这批内容已经分流到 `03_Projects_项目库/价值流作业出图.md`。
- 它是明确的作业/出图交付，不是系统规则，也不是单纯参考资料。
- 该项目当前核心是两套未来态方案：
- `方案一_低成本改善型`
- `方案二_单元化改善型`

### 3. 决策

- 今天没有新增需要写入决策日志的系统级决策。
- 但项目内存在一个待确认选择：最终主推“方案一”还是“方案二”。

### 4. Skill

- 这是一条对 `画图 Skill` 有价值的真实样本。
- 样本的关键特征不是单张出图，而是同时保留 `Mermaid` 可编辑源文件和 `SVG` 可交付文件。
- 目前仍不足以单独升级成正式 Skill，先保留为项目样本。

### 5. 自动化候选

- 今天验证出 `每日 Inbox 整理` 的一个关键边界：不能只看 `今日收集.md`。
- 后续自动化或手工 checklist 都应扫描整个 `00_Inbox_碎片收集` 目录，否则会漏掉子文件夹型输入。

### 6. 原则与长期知识

- 今天没有新增需要写入原则库的新原则。
- 但新增了一条可复用经验：Inbox 的实际扫描单位应是目录，而不是某一个汇总文件。

## 今日值得保留的关键信号

- 这次不是“无新输入日”，而是旧检查路径过窄，漏掉了 Inbox 子目录中的项目资产。
- `Mermaid + SVG` 双层保留方式很实用，兼顾了后续可编辑和最终交付。

## 推荐后续动作

1. 在 `价值流作业出图` 项目中确认最终主推方案。
2. 校对两份图中的中文、数值和指标公式。
3. 为 `每日运行 Skill` 补上“扫描整个 Inbox 目录”的检查项。

## 暂不处理

- 暂不改写 `00_Inbox_碎片收集/今日收集.md` 现有内容。
- 暂不新增决策日志条目。
- 暂不把 `画图 Skill` 候选直接升级为正式 Skill。
