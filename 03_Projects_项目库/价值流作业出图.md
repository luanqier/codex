# 价值流作业出图

## 项目状态

- 状态：进行中
- 日期：2026-05-30
- 输入来源：`00_Inbox_碎片收集/价值流作业出图`
- 交付形式：2 套 SVG 可编辑图 + 1 份 Mermaid 可编辑源文件

## 当前输入摘要

- Inbox 子文件夹中已有 3 份资产：
- `可编辑源文件_Mermaid.md`
- `方案一_低成本改善型_可编辑图.svg`
- `方案二_单元化改善型_可编辑图.svg`
- 内容主题是“价值流作业出图”，包含两案未来态流程图和产线平衡率图。
- Mermaid 源文件已经把两案的流程结构、改善点、关键指标和图表版本保留下来，适合后续继续修改。

## 两案要点

### 方案一：低成本改善型

- 保留原有布局，重点是缩小批量、降低瓶颈工序时间、用 FIFO 和 CIP 限制在制品。
- Mermaid 文件中给出的关键指标包括：
- L/T 约 `11084min -> 4000min`
- P/T 约 `827min -> 804min`
- 产线平衡率约 `47.9% -> 54.5%`
- 更适合低投入、快速交作业的情境。

### 方案二：单元化改善型

- 机加工区改为 U 型单元，同时把热处理、表处、终检、包装串成小批量连续流。
- Mermaid 文件中给出的关键指标包括：
- L/T 约 `11084min -> 2582min`
- P/T 约 `827min -> 782min`
- 增值比约 `7.46% -> 30.3%`
- 产线平衡率约 `68.9%`
- 改善幅度更大，但对布局和协同要求也更高。

## 分类判断

- 这批内容属于明确的项目交付资料，应归入项目库，不属于长期原则、决策日志或正式 Skill 条目。
- 由于内容同时保留了 Mermaid 可编辑源文件和 SVG 交付文件，它也可以作为 `画图 Skill` 的真实样本，但目前样本数还不够，不单独升级为新 Skill。

## 推荐后续动作

1. 确认最终主推的是“方案一”还是“方案二”，避免两案同时提交。
2. 全面校对图中中文文案、符号和数值，重点检查 `TT`、`L/T`、`P/T` 和平衡率。
3. 如果要插入 `PPT` 或 `Word`，再导出一份 `PNG` 或 `PDF` 预览版，确认版式和中文渲染没有问题。
4. 继续保留 Mermaid 作为快速修改版，SVG 作为交付版，不要互相覆盖。
